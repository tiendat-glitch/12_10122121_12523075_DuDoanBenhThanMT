# CKD AI Backend

FastAPI inference service following `Router -> Schema -> Service -> Model Manager -> Artifact`. MongoDB access is isolated in repositories.

Run `pip install -r requirements.txt`, then `uvicorn app.main:app --reload`. Use `pytest` for tests.

